services:
  - type: web
    name: my-spring-app
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    branch: main
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: SPRING_PROFILES_ACTIVE
        value: prod
