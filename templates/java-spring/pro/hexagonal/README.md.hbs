# {{projectName}}

> Generated by [Kybernus CLI](https://kybernus.dev) 🚀 (Pro - Hexagonal Architecture)

## Architecture

This project follows **Hexagonal Architecture** (Ports & Adapters):

```
src/main/java/{{packagePath}}/
├── domain/ # The Hexagon Core
│ ├── entity/ # Business entities
│ ├── repository/ # Outbound port (repository interface)
│ └── usecase/ # Inbound ports (use case interfaces + impl)
├── application/ # Application services
│ └── service/ # Use case orchestration
└── infrastructure/ # Adapters (outside the hexagon)
├── persistence/ # Outbound adapter (database)
├── security/ # Outbound adapter (security)
└── web/ # Inbound adapter (HTTP)
```

## Key Concepts

- **Inbound Ports**: What the application CAN DO (use cases)
- **Outbound Ports**: What the application NEEDS (repositories, external services)
- **Inbound Adapters**: Controllers, CLI, etc.
- **Outbound Adapters**: Database, messaging, external APIs

## Quick Start

```bash
./mvnw spring-boot:run
```

---

Made with ❤️ using [Kybernus Pro](https://kybernus.dev/pro)